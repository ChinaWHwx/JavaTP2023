static关键字可以用来修饰类的成员变量、成员方法和代码块，其主要作用是：

static变量（静态变量）：
属于类，不属于类的任何一个对象。
静态变量只会被初始化一次，在整个程序的运行过程中只存在一份内存，可以通过类名直接访问，不需要创建对象。

static方法（静态方法）：
属于类，不属于类的任何一个对象，可以通过类名直接调用，不需要创建对象。
静态方法不能访问非静态成员变量和非静态成员方法，但是可以调用其他静态方法。

static代码块：
用于对类进行初始化，在类被加载时执行，只执行一次，常用于静态变量的初始化。